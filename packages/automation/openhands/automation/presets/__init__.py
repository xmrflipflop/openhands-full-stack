"""Presets for automation creation.

Presets are ready-to-use automation configurations where users provide
arguments (like a prompt) instead of writing SDK scripts. Each preset
subdirectory contains the necessary files to generate a tarball.

Available presets:
- prompt/: Create an automation from a natural language prompt
"""
